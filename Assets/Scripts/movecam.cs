using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class movecam : MonoBehaviour {

    // Use this for initialization
    void Start () {
        
	}
	
	// Update is called once per frame
	void Update () { //eig ist die Datei ziemlich überflüssig gewordne ....
       //GetComponent<Rigidbody>().velocity = new Vector3(0, GM.vertVel, 4*GM.zVelAdj); // Kamera läuft mit Spieler mit
        //GetComponent<Rigidbody>().velocity = new Vector3(0, 0, 4 * GM.zVelAdj); // Test Janine Kamera 
    }
}
